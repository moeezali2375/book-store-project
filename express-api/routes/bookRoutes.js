import express from "express";

const router = express.Router();

// router.get("/", protect(["reader"]), getAllBooks);

// router.get("/:id", protect(["reader"]), getBookId);

export default router;
